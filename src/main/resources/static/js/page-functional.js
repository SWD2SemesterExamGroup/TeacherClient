/* All overall page creations */

function includeHTML() {
    var elmnt = document.getElementById("menu-bar");
    var attribute = elmnt.getAttribute("include-html");
    console.log(attribute);
    return;
}